# 卡密转码工具网站 - 产品需求文档（PRD）

> 版本：v1.1  |  日期：2026-08-01  |  状态：已定稿（含 P8 可选遥测）

---

## 一、项目概述

### 1.1 项目背景
脉动等饮料厂商在瓶盖内印有卡密字符串（如 `NVD8Y1OHiIyzDMS`），消费者扫码或访问 `https://dbcb2b.cn/S/{卡密}` 即可参与抽奖活动。手动逐个处理卡密效率低下，需要一个批量处理工具，将卡密快速转换为可扫码的二维码或完整活动链接。

### 1.2 项目目标
构建一个**纯前端**的批量卡密转码工具网站，支持：
- 将卡密批量拼接成活动 URL
- 将 URL 批量生成二维码图片
- 适配脉动 + 其他饮料厂商（可扩展）
- 卡密等敏感数据在浏览器本地处理，**零外传**；可选遥测仅收集匿名聚合统计

### 1.3 核心约束
- **纯前端核心**：核心转码功能无后端、无数据库、无第三方接口调用；可选的匿名遥测后端（Cloudflare Workers）仅收集聚合统计，不含卡密
- **隐私优先**：严禁将用户输入的卡密发送到任何外部服务器
- **可部署性**：可一键部署到 GitHub Pages；遥测后端为可选独立部署

---

## 二、目标用户与使用场景

### 2.1 目标用户
- 饮料经销商/批发商（手中有大量瓶盖码需处理）
- 活动推广人员（需批量生成二维码贴标）
- 个人用户（手输卡密不便，想扫码参与）

### 2.2 典型场景
1. **批量贴标场景**：经销商拿到一批瓶盖码，需要生成二维码打印贴到展板上，顾客扫码即参与。
2. **链接分发场景**：将卡密拼接成链接列表，复制到群发工具或表格中分发。
3. **逐个录入场景**：边扫码边录入，实时生成二维码。

---

## 三、功能需求

### 3.1 卡密输入模块

支持三种输入方式（用户可任选）：

| 输入方式 | 说明 | 校验规则 |
|---------|------|---------|
| 文本框批量粘贴 | 多行输入，每行一个卡密；支持逗号/空格/换行分隔 | 自动去重、去空白 |
| 文件上传 | 支持 `.txt`（每行一个）和 `.csv`（第一列卡密）格式 | 文件大小 ≤ 5MB |
| 手动逐个添加 | 输入框 + 添加按钮，逐个加入列表 | 实时格式校验 |

**输入安全要求**（用户明确强调）：
- 输入框使用 `<input type="password">` 类型，卡密以圆点显示
- 页面刷新（F5）或浏览器失焦（blur 到其他窗口）时，**自动清空所有输入内容**
- 设置 `autocomplete="off"`，禁止浏览器自动填充
- 禁止浏览器缓存表单数据（meta 标签禁用缓存）
- 列表中的卡密在界面显示时可脱敏（默认隐藏，点击眼睛图标查看）

### 3.2 厂商管理模块

#### 3.2.1 内置厂商规则
内置脉动厂商规则：

```yaml
脉动:
  名称: 脉动
  URL模板: https://dbcb2b.cn/S/{卡密}
  卡密长度: 15
  字符集: [A-Za-z0-9]
  正则: ^[A-Za-z0-9]{15}$
  图标: bootstrap-icon (cup-straw)
  颜色: #00A859 (脉动绿)

可口可乐:
  名称: 可口可乐
  URL模板: http://i.icoke.cn/KO/{卡密}
  卡密长度: 16
  字符集: [A-Z0-9]
  正则: ^[A-Z0-9]{16}$
  图标: bootstrap-icon (cup-fill)
  颜色: #E61A27 (可口可乐红)

东鹏特饮:
  名称: 东鹏特饮
  URL模板: http://z.5dp.top/{卡密}
  卡密长度: 11
  字符集: [A-Za-z0-9]
  正则: ^[A-Za-z0-9]{11}$
  图标: bootstrap-icon (battery-charging)
  颜色: #0066CC (东鹏蓝)

东鹏激活码:
  名称: 东鹏激活码
  URL模板: http://x.5dp.top/{卡密}
  卡密长度: 11
  字符集: [A-Za-z0-9]
  正则: ^[A-Za-z0-9]{11}$
  图标: bootstrap-icon (lightning-charge)
  颜色: #1E90FF (东鹏激活蓝)

补水啦:
  名称: 补水啦
  URL模板: http://y.5dp.top/{卡密}
  卡密长度: 11
  字符集: [A-Za-z0-9]
  正则: ^[A-Za-z0-9]{11}$
  图标: bootstrap-icon (droplet)
  颜色: #00BFFF (补水啦蓝)

补水啦激活码:
  名称: 补水啦激活码
  URL模板: http://x.5dp.top/{卡密}
  卡密长度: 11
  字符集: [A-Za-z0-9]
  正则: ^[A-Za-z0-9]{11}$
  图标: bootstrap-icon (droplet-half)
  颜色: #87CEEB (补水啦激活蓝)

加多宝:
  名称: 加多宝
  URL模板: HTTPS://S.G4A.CN/{卡密}
  卡密长度: 14
  字符集: [A-Z0-9]
  正则: ^[A-Z0-9]{14}$
  图标: bootstrap-icon (cup-hot)
  颜色: #C8102E (加多宝红)

乐虎:
  名称: 乐虎
  URL模板: http://5l2.cn/d/{卡密}
  卡密长度: 8
  字符集: [A-Za-z0-9]
  正则: ^[A-Za-z0-9]{8}$
  图标: bootstrap-icon (fuel-pump)
  颜色: #F59E0B (乐虎金)

三得利:
  名称: 三得利
  URL模板: https://n.shcb.cc/{卡密}
  卡密长度: 12
  字符集: [A-Za-z0-9]
  正则: ^[A-Za-z0-9]{12}$
  图标: bootstrap-icon (cup)
  颜色: #006400 (三得利绿)

王老吉:
  名称: 王老吉
  URL模板: http://s3.lsa0.cn/N/00F2/200F{卡密}
  卡密长度: 14
  字符集: [A-Z0-9]
  正则: ^[A-Z0-9]{14}$
  图标: bootstrap-icon (cup-hot)
  颜色: #C40022 (王老吉红)
```

> **URL 智能提取**：输入支持纯卡密，也支持完整 URL（如 `https://dbcb2b.cn/S/Bpjlgfs518XMDMS`）。工具会基于当前厂商的 URL 模板反向提取卡密部分，避免把整行 URL 当作卡密拼接成错误链接。

#### 3.2.2 可扩展接口
用户可在「厂商管理」面板自定义添加新厂商，需填写：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 厂商名称 | 文本 | 显示名 | 可口可乐 |
| URL模板 | 文本 | 含 `{卡密}` 占位符 | `https:// Coca.qq.com/s/{卡密}` |
| 卡密正则 | 文本 | 用于格式校验 | `^[A-Z0-9]{12}$` |
| 卡密长度 | 数字 | 提示用 | 12 |
| 图标 | 选择 | Bootstrap Icons 名称 | cup-fill |
| 主题色 | 颜色 | 卡片配色 | #E61A27 |

自定义厂商规则保存在 `localStorage`（仅本地，不外传）。

### 3.3 卡密转码模块（核心）

#### 3.3.1 转码操作
对输入的每个卡密执行：
1. **格式校验** → 合格则拼接 URL → 生成二维码
2. **不合格** → 标记错误原因（长度不符/非法字符），不生成二维码

#### 3.3.2 格式校验规则（仅本地校验）
- ✅ 卡密长度是否符合当前厂商规则
- ✅ 字符集是否符合（字母数字）
- ✅ 是否重复（同批次内）
- ❌ **不**做：跨域请求检测卡密是否中奖/已兑换（CORS 限制 + 隐私要求）

#### 3.3.3 转码结果列表
每条记录显示：
- 序号
- 卡密（脱敏显示，如 `NVD8Y1O***zDMS`，点击查看明文）
- 拼接后的完整 URL
- 生成的二维码缩略图
- 状态徽章（✓有效 / ✗格式错误 / ⚠重复）
- 操作按钮（复制URL、下载二维码、删除）

### 3.4 二维码生成模块

#### 3.4.1 自定义参数
用户可全局配置二维码样式（应用到所有生成的二维码）：

| 参数 | 可选值 | 默认值 |
|------|--------|--------|
| 尺寸 (px) | 128 / 256 / 512 / 自定义 | 256 |
| 容错率 | L (7%) / M (15%) / Q (25%) / H (30%) | M |
| 前景色 | 颜色选择器 | #000000 |
| 背景色 | 颜色选择器 | #FFFFFF |
| 外边距 | 1-10 | 4 |

#### 3.4.2 二维码内容
默认编码完整 URL（如 `https://dbcb2b.cn/S/NVD8Y1OHiIyzDMS`），可在设置中切换为「仅卡密」。

#### 3.4.3 技术实现
- 使用 `qrcode.js`（已本地化到 `assets/lib/qrcode.min.js`，支持离线）
- 二维码中央绘制厂商首字母 + 品牌色 Logo，容错率自动提升至 H 级
- 生成后转为 Canvas，便于下载和打包

### 3.5 输出导出模块

支持三种输出方式（用户已确认全选）：

#### 3.5.1 网格预览 + 逐个下载
- 页面以响应式网格（每行 3-5 个）展示所有二维码卡片
- 每张卡片可独立「下载 PNG」
- 卡片 hover 效果：阴影提升 + 放大 1.02 倍

#### 3.5.2 打包 ZIP 下载
- 一键打包所有有效二维码为 `.zip`
- 文件命名规则：`卡密_厂商名.png`（如 `NVD8Y1OHiIyzDMS_脉动.png`）
- 使用 `JSZip`（已本地化到 `assets/lib/jszip.min.js`，支持离线）

#### 3.5.3 导出 CSV 清单
- 导出包含所有记录的 CSV 文件
- 字段：`序号, 卡密, 厂商, 完整URL, 状态, 生成时间`
- 文件名：`卡密转码清单_YYYYMMDD_HHmmss.csv`
- UTF-8 BOM 编码（保证 Excel 正确识别中文）

### 3.6 安全横幅模块（顶部滚动 Marquee）

#### 3.6.1 功能说明
在页面顶部展示一条醒目的滚动横幅，强调工具的安全性，增强用户信任感。横幅位于 Header 之上，是用户进入页面后看到的第一屏元素。

#### 3.6.2 文案内容
> 🔒 安全承诺：卡密仅在浏览器本地处理，绝不上传服务器；本工具仅上报匿名使用统计（访问量/转码次数/功能使用），不含卡密、URL、二维码等敏感数据。可在设置中关闭。

> **图标策略**：🔒 emoji 与 Bootstrap Icons `bi-lock-fill` 均可接受，以与网站整体美术设计协调一致为准。开发时建议优先采用 Bootstrap Icons `bi-lock-fill`，与全站图标库统一；若整体视觉风格更适合 emoji 也可保留。

#### 3.6.3 样式设计

| 属性 | 值 |
|------|-----|
| 背景色 | 醒目红色渐变 `linear-gradient(90deg,#b91c1c,#dc2626,#ef4444)`，强警示感 |
| 文字色 | 白色高亮 `#FFFFFF` |
| 字重 | 加粗 `font-weight: 700` |
| 字号 | 桌面端 14-16px，移动端 12-14px，确保移动端清晰可读 |
| 内边距 | 上下 10px，左右 16px |
| 图标 | Bootstrap Icons `bi-lock-fill`（替代 emoji，置于文案前） |

#### 3.6.4 交互要求

1. **滚动方式**：平滑的从右向左无限循环滚动（CSS `@keyframes` + `transform: translateX`，双份文案拼接保证无缝循环）
2. **滚动速度**：中等偏慢（约 30-40 秒/循环），便于阅读
3. **悬停暂停**：鼠标 hover 时暂停动画（`animation-play-state: paused`），便于用户停留阅读
4. **关闭按钮**（采用方案 A）：
   - 右侧提供明显的关闭按钮 `×`（Bootstrap Icon `bi-x-lg`）
   - 点击后隐藏横幅
   - 使用 `localStorage` 键名 `safetyBannerDismissed` 记住用户选择，刷新后不再显示
   - 在设置面板提供「显示安全横幅」开关，确保用户可随时恢复显示，避免永久无法找回

#### 3.6.5 布局适配

- 横幅**独立**于核心功能区，位于 Header 之上，使用 `position: relative`（不固定不悬浮），不遮挡下方内容
- 下方内容区自动下移预留空间，不发生重叠
- **窄屏设备**（< 768px）：
  - 自动减小字号至 12px
  - 或改为静态展示（不滚动），文字居中显示
  - 滚动速度自适应，避免移动端滚动过快影响体验

#### 3.6.6 可访问性

- 横幅内容使用 `role="region"` + `aria-label="安全承诺说明"`
- 关闭按钮使用 `aria-label="关闭安全横幅"`
- 尊重 `prefers-reduced-motion`：若用户系统设置减少动画，则静态展示不滚动

---

## 四、非功能需求

### 4.1 安全要求（用户明确强调）
| 编号 | 要求 | 实现方式 |
|------|------|---------|
| S1 | 输入框 password 类型 | `<input type="password">` |
| S2 | 页面刷新自动清空 | `beforeunload` 事件清空输入与列表 |
| S3 | 失焦自动清空 | `window.blur` 事件清空输入框（不清空已生成列表） |
| S4 | 禁止缓存 | `<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">` + `autocomplete="off"` |
| S5 | 卡密零外传 | 卡密、完整 URL、二维码图像**绝不**上报；所有库走 CDN/本地加载脚本但不传用户数据 |
| S6 | 本地存储隔离 | 自定义厂商规则存 `localStorage`，卡密本身**不**存任何位置 |
| S7 | 匿名统计边界 | 仅上报访问量/转码次数/功能使用/厂商 ID/设备类型等**聚合数字**；前端 + 后端双重字段白名单过滤，剥离卡密/URL/二维码等敏感字段；尊重浏览器 DoNotTrack；可在设置中一键关闭 |
| S8 | 已扫状态隔离 | 已扫标记仅存卡密哈希（非明文）到 `sessionStorage`，关闭标签页即清除 |

### 4.2 性能要求
- 单次支持处理 ≤ 1000 个卡密不卡顿
- 二维码生成采用异步分批渲染（每帧 50 个），避免阻塞主线程
- 网格虚拟滚动（>100 条时）

### 4.3 兼容性要求
- 浏览器：Chrome 90+ / Edge 90+ / Firefox 88+ / Safari 14+
- 移动端：响应式适配，支持手机扫码场景
- GitHub Pages 部署：纯静态文件，无需构建

---

## 五、技术方案

### 5.1 技术栈
```
HTML5 + 原生 JavaScript (ES6+) + CSS3
依赖库（本地化到 assets/lib/，支持 PWA 离线，不传用户数据）:
  - qrcode.js        (二维码生成)
  - JSZip            (ZIP 打包)
  - jsQR             (二维码解码，扫码自检/生成验证)
  - SheetJS          (Excel 解析，.xlsx/.xls 导入)
  - Bootstrap Icons  (图标库，CDN + SW 运行时缓存)
可选后端（匿名遥测）:
  - Cloudflare Workers + KV (无服务器，免运维)
  - Chart.js          (看板图表，仅在 stats.html 加载)
```

### 5.2 项目结构
```
Transcoding-Platform/
├── index.html              # 主页面
├── stats.html              # 运营数据看板页面（可选）
├── manifest.json           # PWA 应用清单
├── sw.js                   # Service Worker（离线缓存）
├── css/
│   └── style.css           # 样式表（含横幅/卡片/响应式/暗色模式）
├── js/
│   ├── app.js              # 主逻辑入口（含埋点）
│   ├── input.js            # 输入模块（粘贴/上传/手动 + 安全）
│   ├── vendor.js           # 厂商管理模块
│   ├── transcode.js        # 转码核心逻辑
│   ├── qrcode-gen.js       # 二维码生成（含 Logo 嵌入）
│   ├── export.js           # 导出模块（ZIP/CSV/单图）
│   ├── telemetry-config.js# 遥测配置（端点外置，便于迁移）
│   ├── telemetry.js        # 匿名遥测模块（事件收集/双通道上报）
│   └── stats.js            # 看板逻辑（拉取聚合数据 + Chart.js 渲染）
├── assets/
│   ├── lib/                # 本地化的第三方库
│   ├── icons/              # PWA 图标
│   └── qq/                 # QQ 联系方式配置与二维码
├── workers/                # 可选后端：Cloudflare Workers（匿名遥测）
│   ├── src/                # 后端源码（handlers/lib/constants）
│   └── wrangler.toml       # Wrangler 部署配置
├── README.md
└── PRD.md                  # 本文档
```

### 5.3 UI 设计规范（依据用户偏好）
- **语言**：中文界面
- **布局**：卡片式布局，圆角阴影，hover 微动效
- **图标**：Bootstrap Icons（开源，无 emoji）
- **配色**：以脉动绿 `#00A859` 为主色调，搭配中性灰白
- **响应式**：桌面端三栏（输入/配置/结果），移动端单栏堆叠

### 5.4 页面布局
```
┌─────────────────────────────────────────────────┐
│ [bi-lock-fill] 安全承诺横幅（从右向左滚动）   ×  │
├─────────────────────────────────────────────────┤
│  Header: Logo + 标题 + [看板][设置][客服][GitHub]   │
├─────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│ │ 厂商选择  │ │ 二维码配置 │ │  卡密输入区       │  │
│ │ (脉动▼)  │ │ 尺寸/颜色 │ │  (password)      │  │
│ │ +自定义   │ │ 容错/边距 │ │  粘贴/上传/手动   │  │
│ └──────────┘ └──────────┘ └──────────────────┘  │
├─────────────────────────────────────────────────┤
│  统计栏: 共 N 条 | 有效 M | 无效 K | 重复 L        │
│  操作: [开始转码] [清空] [导出CSV] [打包ZIP]       │
├─────────────────────────────────────────────────┤
│  结果网格 (响应式)                                │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐            │
│  │二维码│ │二维码│ │二维码│ │二维码│ │二维码│            │
│  │卡片  │ │卡片  │ │卡片  │ │卡片  │ │卡片  │            │
│  └────┘ └────┘ └────┘ └────┘ └────┘            │
└─────────────────────────────────────────────────┘
```

---

## 六、开发计划

| 阶段 | 任务 | 产出 |
|------|------|------|
| P1 | 搭建项目骨架、HTML 结构、CSS 基础样式 | 可访问的空页面 |
| P2 | 实现卡密输入模块（粘贴/上传/手动）+ 安全要求 | 输入功能可用 |
| P3 | 实现厂商管理（内置脉动 + 自定义接口） | 厂商切换可用 |
| P4 | 实现转码核心 + 格式校验 + 结果列表 | 转码功能可用 |
| P5 | 集成 qrcode.js + 二维码自定义配置 | 二维码生成可用 |
| P6 | 实现导出（单图/ZIP/CSV） | 导出功能可用 |
| P7 | 移动端响应式 + 交互打磨 + 部署 GitHub Pages | 上线 |
| P8 | 匿名遥测后端 + 前端埋点 + 运营数据看板（详见第九章） | 运营数据可量化，支持简历项目数据展示 |

---

## 七、确认结果（已定稿）

> 以下细节用户已于 2026-07-31 全部按默认值确认，开发阶段以此为准：

1. **失焦清空范围**：✅ 已确认 — 仅清空输入框，保留已生成结果（避免误操作丢失）
2. **卡密脱敏显示**：✅ 已确认 — 默认显示 `NVD8Y1O***zDMS`，点击眼睛图标显示明文
3. **二维码内容**：✅ 已确认 — 编码完整 URL（扫码即跳转活动页）
4. **是否记录使用历史**：✅ 已确认 — 否（隐私优先，关闭即清除，不持久化卡密）
5. **主题色**：✅ 已确认 — 脉动绿 `#00A859` 为主色调
6. **安全横幅图标**：✅ 已确认 — emoji / Bootstrap Icons 均可，以美术协调性为准
7. **安全横幅关闭恢复**：✅ 已确认 — 方案 A，设置面板提供「显示安全横幅」恢复开关

---

## 八、风险与说明

| 风险 | 说明 | 应对 |
|------|------|------|
| CORS 限制 | 纯前端无法真正检测卡密有效性 | 已与用户确认改为「仅格式校验」 |
| 第三方依赖 | qrcode/JSZip/jsQR/SheetJS 已本地化；Bootstrap Icons/Chart.js 走 CDN | 核心库已本地化，CDN 库经 SW 运行时缓存，离线基本可用 |
| 大批量性能 | 1000+ 卡密可能卡顿 | 分批渲染 + IntersectionObserver 虚拟滚动 |
| 隐私合规 | 卡密可能涉及活动权益 | 全本地处理，不持久化卡密；匿名统计仅聚合数字，双重白名单过滤，可一键关闭 |
| 遥测后端依赖 | 看板依赖 Cloudflare Workers | 后端为可选；未部署时主页功能不受影响，看板显示错误状态 |

---

## 九、匿名遥测与统计看板（P8，可选能力）

> 目标：在不触碰卡密等敏感数据的前提下，收集匿名使用统计，供运营复盘与简历项目数据展示。

### 9.1 隐私边界（强制约束）

- **绝不收集**：卡密明文、完整 URL、二维码图像/dataURL、QQ 号、IP、Cookie、Token 等敏感字段
- **仅收集聚合数字**：访问量、转码次数、转码卡密数（有效/无效/重复）、厂商 ID、功能使用次数、设备类型（mobile/pc）、来源类型（direct/internal/域名）
- **双重过滤**：前端 `telemetry.js` 与后端 `workers/src/lib/validate.js` 各维护一份字段白名单 + 敏感字段黑名单，前端剥离后后端再剥一次
- **用户控制**：尊重浏览器 DoNotTrack；设置面板提供「匿名使用统计」开关，一键 opt-out 并持久化到 localStorage

### 9.2 事件埋点（13 处）

| 事件类型 | 字段 | 触发点 |
|---------|------|--------|
| pageview | referrer, device, tz | `TelemetryModule.init()` 自动触发 |
| transcode | vendorId, total, valid, invalid, duplicate, preview | 批量转码完成 / 单条预览 |
| feature | name | 粘贴导入、文件导入、手动添加、扫码自检、CSV 导出、ZIP 打包、打印、新增厂商、导出/导入规则、标记已扫 |

### 9.3 后端数据模型（Cloudflare KV）

- **日聚合** `daily:YYYY-MM-DD`：当日 pageview/transcode/transcodeCards/... + vendors/features/devices/referrers 计数 map
- **缓存聚合** `agg:cached`：近 30 天日聚合合并快照，60 秒懒刷新（过期则遍历重算）
- **可迁移性**：所有 KV 访问封装在 `workers/src/lib/storage.js`，迁移到自建后端只需实现同接口

### 9.4 看板页面（`stats.html` + `js/stats.js`）

- **KPI 指标卡**：累计访问量、转码次数、转码卡密数、有效/无效/重复卡密
- **趋势图**：近 30 天访问量 + 转码次数双折线（Chart.js）
- **分布图**：厂商转码分布（柱状）、设备分布（环形）、访问来源分布（环形）
- **功能排行**：进度条列表，Top3 高亮
- **交互**：60 秒自动刷新、手动刷新、暗色模式自适应、加载/错误/空状态降级
- **端点复用**：看板从 `telemetry-config.js` 推导 API base，迁移时只改一处

### 9.5 Service Worker 守卫

- `sw.js` 对 `/api/` 路径直接放行（不拦截、不缓存），确保遥测上报与看板数据查询始终走网络直连后端
- 看板与遥测脚本已加入 SW 预缓存列表（`CACHE = cardtool-v4`）
