# 卡密转码工具 - 开发问题与解决方案

> 本文档记录了项目开发过程中遇到的典型问题、根因分析及解决方案，供后续维护和类似项目参考。

---

## 目录

1. [厂商规则配置错误](#1-厂商规则配置错误)
2. [文件上传交互问题](#2-文件上传交互问题)
3. [手机端按钮布局溢出](#3-手机端按钮布局溢出)
4. [遥测数据丢失](#4-遥测数据丢失)
5. [Cloudflare Workers 大量错误](#5-cloudflare-workers-大量错误)
6. [响应式适配问题](#6-响应式适配问题)
7. [隐私合规设计](#7-隐私合规设计)
8. [PWA 离线缓存策略](#8-pwa-离线缓存策略)
9. [性能优化问题](#9-性能优化问题)
10. [部署与运维问题](#10-部署与运维问题)

---

## 1. 厂商规则配置错误

### 1.1 问题描述

用户反馈：输入正确格式的卡密 URL 后，转码仍然失败。

**具体案例**：
- 东鹏特饮卡密 `http://z.5dp.top/xj6jmgeNIXh` 无法转码
- 东鹏激活码卡密 `http://x.5dp.top/GWHYdhUUp1f` 无法转码
- 补水啦、补水啦激活码同样失败

### 1.2 问题原因

**根因**：厂商规则配置与参考文件不一致，涉及两类错误：

| 厂商 | 错误类型 | 具体表现 |
|------|----------|----------|
| 东鹏特饮 | URL 模板互换 | 配置为 `http://x.5dp.top/`，实际应为 `http://z.5dp.top/` |
| 东鹏激活码 | URL 模板互换 | 配置为 `http://z.5dp.top/`，实际应为 `http://x.5dp.top/` |
| 东鹏特饮/激活码 | 卡密长度错误 | 配置为 12 位，实际参考文件为 11 位 |
| 补水啦/激活码 | 卡密长度错误 | 配置为 12 位，实际参考文件为 11 位 |

**影响**：
- URL 模板错误 → 生成的链接指向错误域名
- 长度校验错误 → 所有输入都被判定为"格式不符"，转码 100% 失败

### 1.3 解决方案

**修复步骤**：

1. **核对参考文件**：读取 `chankaoURL/` 目录下的真实卡密文件，提取 URL 和卡密长度
2. **修正 vendor.js**：
   ```javascript
   // 东鹏特饮
   {
     id: 'dongpeng',
     urlTemplate: 'http://z.5dp.top/{卡密}',  // x → z
     regex: '^[A-Za-z0-9]{11}$',              // 12 → 11
     length: 11,                              // 12 → 11
   }
   
   // 东鹏激活码
   {
     id: 'dongpeng-active',
     urlTemplate: 'http://x.5dp.top/{卡密}',  // z → x
     regex: '^[A-Za-z0-9]{11}$',              // 12 → 11
     length: 11,                              // 12 → 11
   }
   ```
3. **同步更新 PRD.md**：确保文档与代码一致

**验证方法**：
- 输入参考文件中的真实卡密，验证转码成功
- 输入错误长度的卡密，验证校验失败并提示正确长度

### 1.4 经验教训

1. **配置与参考文件必须一一核对**：不能凭记忆或假设填写，必须逐字段对比
2. **建立配置校验机制**：可在 `vendor.js` 中添加单元测试，用参考文件中的卡密自动验证规则
3. **文档与代码同步更新**：修改配置后必须同步更新 PRD.md，避免文档过时

---

## 2. 文件上传交互问题

### 2.1 问题描述

**问题 A**：用户选择文件后，界面无任何反馈，不知道是否选成功。

**问题 B**：选择文件后点击"解析并添加"按钮，提示"请先选择文件"，无法解析。

### 2.2 问题原因

**问题 A 原因**：`fileInput` 的 `change` 事件未监听，用户选文件后无 UI 反馈。

**问题 B 原因**：`fileInputChangeHandler` 用 `innerHTML` 替换内容时，把 `<input type="file">` 元素也销毁了。虽然重建了一个新 input，但用户选的文件在旧 input 上，新 input 是空的 → `fileInput.files.length === 0`。

**代码还原**：

```javascript
// ❌ 错误写法：innerHTML 替换会销毁 input 元素
function fileInputChangeHandler() {
  if (fileDrop) {
    fileDrop.classList.add('has-file');
    fileDrop.innerHTML =
      '<i class="bi bi-file-earmark-check"></i>' +
      '<span class="file-name">文件名</span>' +
      '<input type="file" id="fileInput" hidden>';  // 新 input，文件丢失
  }
}
```

### 2.3 解决方案

**修复步骤**：

1. **添加 change 事件监听**：选文件后立即显示文件名和大小
2. **保留原始 input 元素**：替换 innerHTML 前先取走 input，替换后再放回

```javascript
// ✅ 正确写法：先取走 input，替换后再放回
function fileInputChangeHandler() {
  if (!fileInput || !fileInput.files || !fileInput.files.length) return;
  var file = fileInput.files[0];
  var name = file.name || '未知文件';
  var size = file.size || 0;
  var sizeStr = size < 1024 ? size + 'B' : (size < 1024 * 1024 ? (size / 1024).toFixed(1) + 'KB' : (size / 1024 / 1024).toFixed(1) + 'MB');
  
  if (fileDrop) {
    fileDrop.classList.add('has-file');
    // 先取走 fileInput 再替换 innerHTML，保留用户已选的文件
    var input = fileInput;
    if (input && input.parentNode === fileDrop) fileDrop.removeChild(input);
    fileDrop.innerHTML =
      '<i class="bi bi-file-earmark-check"></i>' +
      '<span class="file-name"><i class="bi bi-paperclip"></i>' + name + ' (' + sizeStr + ')</span>' +
      '<span style="font-size:12px;opacity:0.7">点击「解析并添加」或重新选择文件</span>';
    // 放回同一个 input（保留了用户选的文件）
    if (input) fileDrop.appendChild(input);
  }
}
```

3. **解析完成后恢复初始状态**：

```javascript
function resetFileDrop() {
  if (fileDrop) {
    fileDrop.classList.remove('has-file');
    // 先取走 fileInput 再替换 innerHTML，避免销毁 input 元素
    var input = fileInput;
    if (input && input.parentNode === fileDrop) fileDrop.removeChild(input);
    fileDrop.innerHTML =
      '<i class="bi bi-cloud-arrow-up"></i>' +
      '<span>点击选择或拖入文件</span>';
    // 重新清空 input 的 value 并放回
    if (input) {
      try { input.value = ''; } catch (e) {}
      fileDrop.appendChild(input);
    }
  }
}
```

### 2.4 经验教训

1. **innerHTML 替换会销毁子元素**：包括 input 的 value、event listener 等
2. **需要保留 DOM 元素时**：用 `removeChild` 取走，替换后再 `appendChild` 放回
3. **文件上传必须有即时反馈**：显示文件名、大小，让用户确认选成功

---

## 3. 手机端按钮布局溢出

### 3.1 问题描述

手机端（≤768px）操作栏的 6 个按钮（开始转码、预览首条、清空结果、导出 CSV、打包 ZIP、打印）在一行平分，每个按钮宽度约 49px，文字溢出屏幕。

### 3.2 问题原因

**根因**：移动端断点内 `.actions .btn` 样式设置不合理：

```css
/* ❌ 错误写法 */
.actions .btn {
  flex: 1;          /* 6 个按钮平均分配一行宽度 */
  min-width: 0;     /* 允许缩小到 0，不被内容撑开 */
  padding: 11px 12px;
}
```

**计算验证**：
- 手机屏幕宽度：约 375px
- `.action-bar` 两侧 padding：各 20px → 可用宽度 335px
- `.actions` gap：8px × 5 = 40px
- 6 个按钮平分：(335 - 40) ÷ 6 ≈ **49px/按钮**
- 按钮内容：图标 (20px) + gap(6px) + "开始转码"(4 字×约 14px) ≈ **82px**
- 溢出：82px > 49px ✅

**加剧因素**：
1. `.btn` 基类的 `white-space: nowrap` → 文字不换行，强制溢出
2. 移动端断点内 `.actions` 未显式声明 `flex-wrap: wrap` → 依赖基础样式继承
3. `min-width: 0` → 按钮无法被内容撑开，文字只能溢出

### 3.3 解决方案

**修复步骤**：

修改 [style.css](file:///e:/practice/AIbox-CN/Transcoding-Platform/css/style.css#L1422-L1433) 第 1422-1433 行：

```css
/* ✅ 正确写法 */
.actions {
  justify-content: stretch;
  flex-wrap: wrap;  /* 显式允许换行 */
}

.actions .btn {
  flex: 1 1 calc(33.333% - 4px);  /* 每行 3 个按钮 */
  min-width: calc(33.333% - 4px); /* 固定最小宽度 */
  padding: 10px 6px;              /* 更紧凑的内边距 */
  font-size: 12px;                /* 适配小按钮的字号 */
}
```

**效果**：
- 6 个按钮自动变成 **2 行 × 3 列**
- 每个按钮宽度约 1/3 屏幕（约 105px），文字不再溢出

### 3.4 经验教训

1. **移动端按钮布局必须考虑文字宽度**：不能简单用 `flex: 1` 平分
2. **显式声明 flex-wrap**：不要依赖基础样式继承，断点内应明确声明
3. **建立布局测试机制**：用 `test-layout.html` 模拟不同屏幕宽度验证布局

---

## 4. 遥测数据丢失

### 4.1 问题描述

用户反馈：今天已知至少有一人访问网站进行了转码，但数据面板没有显示转码次数增加。

**现象**：
- 访问量（pageview）在涨
- 转码次数（transcode）不涨
- 后端 `/api/track` 接口正常，CORS 配置正确

### 4.2 问题原因

**根因**：前端遥测 flush 机制不可靠，单次转码事件极易丢失。

**遥测上报流程**：

```
用户操作 → track() 入队 → 等待 flush → 发送到后端
                    ↓
         三种 flush 触发条件：
         1. 队列满 10 条（batchSize）
         2. 5 秒定时器
         3. 页面卸载时（pagehide/visibilitychange）
```

**问题分析**：

| 触发条件 | 是否可靠 | 原因 |
|----------|----------|------|
| 队列满 10 条 | ❌ 不可靠 | 一次转码只产生 1 个事件，远达不到 10 条阈值 |
| 5 秒定时器 | ⚠️ 部分可靠 | 用户转码后若停留 ≥5 秒可成功，但"转完即走"会丢失 |
| 页面卸载 flush | ❌ 不可靠 | 用普通 `fetch`，浏览器在页面卸载时通常会取消请求 |

**真实场景**：
1. 用户访问网站 → `pageview` 事件入队
2. 用户停留 5 秒以上 → 定时器 flush → **pageview 成功上报** ✅
3. 用户执行转码 → `transcode` 事件入队
4. 用户看了一眼二维码，几秒内关闭标签页
5. 5 秒定时器还没到 → 卸载 flush 触发但 fetch 被浏览器取消
6. **transcode 事件丢失** ❌

**代码还原**：

```javascript
//  错误写法：转码事件只入队，等 5 秒定时器
function track(event, props) {
  queue.push({ event: event, ts: Date.now(), props: clean });
  // 队列满立即 flush
  if (queue.length >= c.batchSize) {
    flush();
  }
  // 没有立即 flush 机制！
}

// ❌ 错误写法：卸载时用普通 fetch
function flush() {
  fetch(c.endpoint, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ events: queue })
    // 没有 keepalive，页面卸载时请求被取消
  });
}
```

### 4.3 解决方案

**修复步骤**：

1. **低频高价值事件立即 flush**：

```javascript
// ✅ 正确写法：转码事件入队后立即 flush
var IMMEDIATE_FLUSH_EVENTS = { transcode: true };

function track(event, props) {
  queue.push({ event: event, ts: Date.now(), props: clean });
  if (queue.length >= c.batchSize) {
    flush();
  } else if (IMMEDIATE_FLUSH_EVENTS[event]) {
    // 低频高价值事件（如转码）：立即 flush，不等 5 秒定时器
    flush();
  }
}
```

2. **卸载 flush 改用 sendBeacon**：

```javascript
// ✅ 正确写法：卸载时用 sendBeacon（浏览器保证发出）
function flush(useBeacon) {
  var payload = JSON.stringify({ events: queue });
  
  if (useBeacon) {
    // 卸载时优先用 sendBeacon（text/plain 类型避免 CORS 预检）
    if (navigator.sendBeacon) {
      var blob = new Blob([payload], { type: 'text/plain' });
      if (!navigator.sendBeacon(c.endpoint, blob)) {
        sendFetch(payload);  // 回退 fetch
      }
    } else {
      sendFetch(payload);
    }
  } else {
    sendFetch(payload);
  }
  
  queue = [];  // 无论是否成功都清空队列
}

// 页面卸载时调用
window.addEventListener('pagehide', function () { flush(true); });
document.addEventListener('visibilitychange', function () {
  if (document.visibilityState === 'hidden') flush(true);
});
```

3. **后端添加 Cache-Control**：

```javascript
// ✅ 正确写法：API 响应加 no-store，防止边缘缓存
export function json(data, req, env, status) {
  var headers = Object.assign(
    { 'Content-Type': 'application/json; charset=utf-8' },
    corsHeaders(req, env)
  );
  headers['Cache-Control'] = 'no-store';  // 防止浏览器/边缘缓存
  return new Response(JSON.stringify(data), {
    status: status || 200,
    headers: headers
  });
}
```

### 4.4 经验教训

1. **低频高价值事件必须立即上报**：不能依赖定时器或批量
2. **页面卸载时用 sendBeacon**：普通 fetch 会被浏览器取消
3. **sendBeacon 用 text/plain 类型**：避免 CORS 预检（预检在卸载时来不及完成）
4. **API 响应加 Cache-Control: no-store**：防止边缘缓存导致看板数据滞后

---

## 5. Cloudflare Workers 大量错误

### 5.1 问题描述

用户在 Cloudflare 控制台首页看到大量 Workers 错误，错误计数持续累积。

### 5.2 问题原因

**根因**：后端代码缺少全局异常兜底，KV 操作裸调用，任何异常直接计入 Cloudflare 错误计数。

**具体问题**：

| 问题 | 位置 | 影响 |
|------|------|------|
| 入口 fetch 无全局 try/catch | `index.js:13-52` | handler 内部任何异常都变成未捕获异常 |
| KV 操作裸调用 | `storage.js` 所有函数 | KV 瞬时错误沿调用栈抛出 |
| `applyEvent` 假设嵌套 map 存在 | `track.js:62-86` | 旧版文档缺失字段时抛 TypeError |
| 每次 track 都读 meta | `track.js:35-38` | 增加 KV 读写次数和失败面 |

**代码还原**：

```javascript
// ❌ 错误写法：入口无 try/catch
export default {
  async fetch(request, env, ctx) {
    var url = new URL(request.url);
    var path = url.pathname;
    
    if (path === '/api/track') {
      return await handleTrack(request, env);  // 裸调用，异常直接抛出
    }
    // ...
  }
};

// ❌ 错误写法：KV 操作无防护
export async function getDaily(env, dateStr) {
  var raw = await env.STATS.get(dateKey(dateStr));  // 裸调用
  if (!raw) return emptyDaily(dateStr);
  return JSON.parse(raw);  // 解析失败直接抛出
}

// ❌ 错误写法：假设嵌套 map 存在
function applyEvent(doc, evt) {
  doc.devices[dev] = (doc.devices[dev] || 0) + 1;  // doc.devices 可能是 undefined
  doc.vendors[vid] = (doc.vendors[vid] || 0) + 1;  // doc.vendors 可能是 undefined
}
```

### 5.3 解决方案

**修复步骤**：

1. **入口添加全局 try/catch**：

```javascript
// ✅ 正确写法
export default {
  async fetch(request, env, ctx) {
    var url;
    try { url = new URL(request.url); }
    catch (e) { return new Response('Bad Request', { status: 400 }); }
    
    try {
      // 路由分发
      if (path === '/api/track') {
        return await handleTrack(request, env);
      }
      // ...
    } catch (err) {
      console.error('[worker-error]', err && err.message);
      return json({ ok: false, error: 'internal' }, request, env, 500);
    }
  }
};
```

2. **KV 操作加防护**：

```javascript
// ✅ 正确写法
export async function getDaily(env, dateStr) {
  if (!env || !env.STATS) return emptyDaily(dateStr);
  try {
    var raw = await env.STATS.get(dateKey(dateStr));
    if (!raw) return emptyDaily(dateStr);
    var doc = JSON.parse(raw);
    // 结构补全：旧版文档可能缺失嵌套 map
    doc.vendors = doc.vendors || {};
    doc.features = doc.features || {};
    doc.devices = doc.devices || {};
    doc.referrers = doc.referrers || {};
    return doc;
  } catch (e) {
    return emptyDaily(dateStr);
  }
}
```

3. **优化 meta 读取**：

```javascript
// ✅ 正确写法：仅当天文档为空时才检查 meta
if (doc.pageview === 0 && doc.transcode === 0) {
  var since = await getMeta(env, 'since');
  if (!since) {
    await putMeta(env, 'since', { since: dateStr, version: 1 });
  }
}
```

### 5.4 经验教训

1. **Serverless 函数必须有全局异常兜底**：任何未捕获异常都会计入错误计数
2. **KV 操作必须加 try/catch**：KV 服务可能有瞬时错误
3. **读取外部数据后做结构补全**：不能假设数据结构永远完整
4. **减少不必要的 KV 读写**：降低失败面和配额消耗

---

## 6. 响应式适配问题

### 6.1 问题描述

- QQ 弹窗图片尺寸过小，手机端看不清
- 设置弹窗内容滚动截断，底部控件被遮挡
- 全面屏安全区未适配，内容被刘海/底部横条遮挡

### 6.2 问题原因

| 问题 | 原因 |
|------|------|
| QQ 弹窗图片小 | 固定宽度 `min(320px, 72vw)`，手机端实际显示约 270px |
| 设置弹窗截断 | `.modal-content` 整体滚动，header/footer 跟着滚动消失 |
| 安全区未适配 | 未使用 `env(safe-area-inset-*)` |

### 6.3 解决方案

**QQ 弹窗放大**：

```css
/* ✅ 修改后 */
.qq-img-wrap {
  width: min(340px, 80vw);   /* 320px → 340px, 72vw → 80vw */
  height: min(340px, 80vw);
}
```

**设置弹窗滚动优化**：

```css
/* ✅ 修改后 */
.modal-content {
  display: flex;
  flex-direction: column;
  max-height: 90vh;
  overflow: hidden;  /* 整体不滚动 */
}

.modal-body {
  flex: 1;
  overflow-y: auto;  /* 只有 body 滚动 */
}
```

**全面屏安全区适配**：

```css
/* ✅ 修改后 */
.header {
  padding: max(16px, env(safe-area-inset-top)) 
           max(24px, env(safe-area-inset-right)) 
           16px 
           max(24px, env(safe-area-inset-left));
}

.safety-banner {
  padding: max(10px, env(safe-area-inset-top)) 
           56px 
           10px 
           max(16px, env(safe-area-inset-left));
}
```

### 6.4 经验教训

1. **弹窗用 flex 列布局**：header/footer 固定，body 独立滚动
2. **图片尺寸用 vw 单位**：适配不同屏幕宽度
3. **全面屏必须适配安全区**：使用 `env(safe-area-inset-*)`

---

## 7. 隐私合规设计

### 7.1 问题描述

如何在不收集用户敏感数据的前提下，实现运营数据追踪？

### 7.2 解决方案

**双重白名单过滤机制**：

1. **前端第一道过滤**：

```javascript
// 事件字段白名单
var EVENT_FIELDS = {
  pageview: ['referrer', 'device', 'tz'],
  transcode: ['vendorId', 'total', 'valid', 'invalid', 'duplicate', 'preview'],
  feature: ['name'],
  setting: ['name', 'value']
};

// 敏感字段黑名单
var SENSITIVE_KEYS = [
  'code', 'codes', 'url', 'dataURL', 'regex', 'urlTemplate',
  'qq', 'phone', 'email', 'token', 'password', 'cookie', 'ip'
];

function sanitizeProps(event, props) {
  var allowed = EVENT_FIELDS[event];
  if (!allowed) return null;
  var clean = {};
  Object.keys(props).forEach(function (k) {
    if (SENSITIVE_KEYS.indexOf(k) >= 0) return;  // 敏感字段丢弃
    if (allowed.indexOf(k) >= 0) clean[k] = props[k];
  });
  return clean;
}
```

2. **后端第二道过滤**：

```javascript
// workers/src/lib/validate.js
export function sanitizeEvent(evt) {
  var event = String(evt.event || '').toLowerCase();
  var allowed = EVENT_FIELDS[event];
  if (!allowed) return { valid: false, reason: 'unknown_event' };
  
  var props = {};
  Object.keys(evt.props || {}).forEach(function (k) {
    if (SENSITIVE_KEYS.indexOf(k) >= 0) {
      warnings.push('dropped_sensitive:' + k);
      return;
    }
    if (allowed.indexOf(k) >= 0) {
      props[k] = evt.props[k];
    }
  });
  
  return { valid: true, event: event, props: props };
}
```

3. **用户可控 opt-out**：

```javascript
function isOptedOut() {
  if (localStorage.getItem('telemetry_opt_out') === '1') return true;
  var dnt = navigator.doNotTrack || window.doNotTrack;
  if (dnt === '1' || dnt === 'yes') return true;
  return false;
}
```

### 7.3 经验教训

1. **隐私设计必须前后端双重过滤**：不能只依赖前端
2. **白名单 + 黑名单双保险**：白名单限制允许字段，黑名单防止敏感字段漏网
3. **尊重用户选择**：支持 DoNotTrack 和手动 opt-out

---

## 8. PWA 离线缓存策略

### 8.1 问题描述

CDN 依赖（qrcode.js、JSZip 等）加载失败时，功能静默失效，用户无感知。

### 8.2 解决方案

**Service Worker 缓存策略**：

```javascript
// sw.js
var CACHE = 'cardtool-v4';

var PRECACHE = [
  './',
  'index.html',
  'css/style.css',
  'js/vendor.js',
  'js/input.js',
  'js/transcode.js',
  'js/qrcode-gen.js',
  'js/export.js',
  'js/app.js',
  'js/telemetry.js',
  'js/telemetry-config.js',
  'assets/lib/qrcode.min.js',      // 本地化 CDN 库
  'assets/lib/jszip.min.js',
  'assets/lib/jsQR.js',
  'assets/lib/xlsx.full.min.js',
  'assets/icons/icon.svg',
  'manifest.json'
];

// CDN 资源：cache-first，后台更新
if (isCdn) {
  e.respondWith(
    caches.match(req).then(function (cached) {
      var fetcher = fetch(req).then(function (res) {
        if (res && res.status === 200) {
          var copy = res.clone();
          caches.open(CACHE).then(function (c) { c.put(req, copy); });
        }
        return res;
      }).catch(function () { return cached; });
      return cached || fetcher;
    })
  );
}

// 本地资源：network-first，失败回退缓存
else {
  e.respondWith(
    fetch(req).then(function (res) {
      if (res && res.status === 200) {
        var copy = res.clone();
        caches.open(CACHE).then(function (c) { c.put(req, copy); });
      }
      return res;
    }).catch(function () {
      return caches.match(req);
    })
  );
}
```

**依赖加载失败提示**：

```javascript
// app.js
function checkDependencies() {
  var deps = [
    { name: 'QRCode', test: typeof QRCode !== 'undefined' },
    { name: 'JSZip', test: typeof JSZip !== 'undefined' },
    { name: 'jsQR', test: typeof jsQR !== 'undefined' },
    { name: 'XLSX', test: typeof XLSX !== 'undefined' }
  ];
  
  var failed = deps.filter(function (d) { return !d.test; });
  if (failed.length > 0) {
    var names = failed.map(function (d) { return d.name; }).join('、');
    showDepErrorBanner(names + ' 加载失败，相关功能不可用');
  }
}
```

### 8.3 经验教训

1. **CDN 库本地化**：核心依赖下载到本地，避免 CDN 不可用
2. **Service Worker 缓存 CDN**：首次在线后离线可用
3. **依赖加载失败必须有提示**：不能静默失效

---

## 9. 性能优化问题

### 9.1 问题描述

大批量卡密（1000+）一次性渲染导致 DOM 节点过多，滚动卡顿。

### 9.2 解决方案

**虚拟滚动 + IntersectionObserver**：

```javascript
// 分页渲染，每批 50 个
var PAGE_SIZE = 50;
var currentPage = 0;
var allResults = [];

function renderResults(results) {
  allResults = results;
  currentPage = 0;
  renderPage();
}

function renderPage() {
  var start = currentPage * PAGE_SIZE;
  var end = start + PAGE_SIZE;
  var page = allResults.slice(start, end);
  
  page.forEach(function (r) {
    var card = createQrCard(r);
    resultGrid.appendChild(card);
  });
  
  currentPage++;
  
  // 如果还有更多，设置哨兵触发下一批
  if (end < allResults.length) {
    setupSentinel();
  }
}

function setupSentinel() {
  var sentinel = document.createElement('div');
  sentinel.className = 'result-sentinel';
  resultGrid.appendChild(sentinel);
  
  var observer = new IntersectionObserver(function (entries) {
    if (entries[0].isIntersecting) {
      observer.disconnect();
      sentinel.remove();
      renderPage();
    }
  });
  observer.observe(sentinel);
}
```

### 9.3 经验教训

1. **大批量数据必须分页渲染**：不能一次性插入 DOM
2. **IntersectionObserver 比 scroll 事件高效**：无需监听滚动，自动触发
3. **哨兵元素占满整行**：确保在网格布局中正确触发

---

## 10. 部署与运维问题

### 10.1 问题描述

- 根目录 `wrangler.toml` 与 `workers/wrangler.toml` 配置冲突
- Service Worker 缓存导致页面更新后用户看不到新代码
- GitHub Pages 部署延迟约 1-2 分钟

### 10.2 解决方案

**删除冗余配置文件**：

```bash
# 删除根目录的 wrangler.toml，统一使用 workers/ 下的配置
rm wrangler.toml
```

**Service Worker 版本控制**：

```javascript
// sw.js
var CACHE = 'cardtool-v4';  // 每次更新 bump 版本号

self.addEventListener('activate', function (e) {
  e.waitUntil(
    caches.keys().then(function (keys) {
      return Promise.all(
        keys.filter(function (k) { return k !== CACHE; })
            .map(function (k) { return caches.delete(k); })
      );
    }).then(function () {
      return self.clients.claim();
    })
  );
});
```

**用户侧强制刷新提示**：

```javascript
// app.js
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.ready.then(function (reg) {
    reg.addEventListener('updatefound', function () {
      var newWorker = reg.installing;
      newWorker.addEventListener('statechange', function () {
        if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
          toast('检测到新版本，刷新页面以更新', 'info');
        }
      });
    });
  });
}
```

### 10.3 经验教训

1. **配置文件唯一性**：避免多处配置冲突
2. **Service Worker 版本号管理**：每次更新必须 bump 版本号
3. **提示用户强制刷新**：`Ctrl+Shift+R` 清除旧缓存

---

## 总结

### 核心经验

1. **配置必须与参考文件一一核对**：不能凭记忆或假设
2. **DOM 操作要注意元素生命周期**：innerHTML 替换会销毁子元素
3. **移动端布局必须实测**：不能只在桌面端调试
4. **遥测数据必须立即上报**：低频高价值事件不能等批量
5. **Serverless 函数必须有全局异常兜底**：任何异常都会计入错误计数
6. **隐私设计必须前后端双重过滤**：不能只依赖前端
7. **CDN 依赖必须本地化 + 缓存**：避免 CDN 不可用
8. **大批量数据必须分页渲染**：避免 DOM 节点过多
9. **配置文件必须唯一**：避免多处配置冲突
10. **Service Worker 必须版本控制**：确保用户拿到最新代码

### 项目数据

- **开发周期**：约 2 周（2026-07-31 ~ 2026-08-14）
- **代码行数**：约 3000 行（HTML + CSS + JS）
- **厂商数量**：10 个内置厂商
- **功能模块**：11 个（厂商管理、输入处理、转码核心、二维码生成、导出、遥测、看板等）
- **部署平台**：GitHub Pages + Cloudflare Workers
- **累计访问量**：21+（截至 2026-08-04）
- **累计转码次数**：24+（截至 2026-08-04）
- **处理卡密总量**：185+（截至 2026-08-04）

---

*文档版本：v1.0*  
*最后更新：2026-08-14*
